# 《归航：蛊界异客》本阶段前端开发文档

> 文档版本：v0.1  
> 文档状态：P1 开发基线  
> 适用阶段：P1 网页单章试玩  
> 目标章节：第一章《坍塌之下》  
> 更新日期：2026-08-25

关联文档：

- [`PRD.md`](./PRD.md)
- [`前端技术适配声明.md`](./前端技术适配声明.md)

## 1. 本阶段目标

### 1.1 交付目标

交付一个可通过网页链接分享的单人 AI 对话情景 RPG 试玩版。玩家无需注册账号，可以在手机或电脑浏览器中完成第一章《坍塌之下》，刷新后继续当前进度，并在章节结束后提交试玩反馈。

### 1.2 本阶段验证问题

1. 玩家是否愿意主动询问并信赖“归航”；
2. 玩家是否能理解“询问”和“执行”的区别；
3. 自由输入是否能产生可解释、有代价的行动结果；
4. 状态、物品、NPC 关系和剧情因果是否可信；
5. 玩家是否愿意继续进入第二章。

### 1.3 本阶段完成标志

- 可生成并分享公网试玩链接；
- 匿名玩家可以开始、继续、重开和完成第一章；
- 第一章关键节点、失败和结局能够正常触发；
- AI 流式消息、规则结算和状态更新保持一致；
- 至少完成 5 名中国大陆玩家的封闭试玩；
- 严重状态错误每局不超过 1 次。

## 2. 本阶段范围

### 2.1 本阶段包含

- 游戏首页、开场介绍、游戏页、结算页、反馈页；
- 第一章完整对话流程；
- 询问归航、执行行动、与 NPC 交流；
- 快捷行动与自由输入；
- 不可逆行动确认和 D6 公开检定；
- 结构化流式 AI 对话；
- 状态、背包、人物关系、档案与归乡计划；
- 匿名玩家与同一浏览器续玩；
- 死亡、被捕、共同逃生、独自逃生等结算；
- 固定背景图、归航头像、轻量打字效果、默认关闭的环境音；
- 基础埋点、错误日志和模型失败提示。

### 2.2 本阶段不包含

- 第二章和第三章；
- 注册登录和跨设备存档；
- 多人、排行榜、交易和公共世界；
- 实时 AI 生图、配音和复杂动画；
- 机甲驾驶、实时战斗或开放地图；
- 商城、付费、广告、UGC 和运营后台。

## 3. 技术基线

| 项目 | 本阶段采用 |
|---|---|
| 框架 | Next.js App Router |
| 语言 | TypeScript 严格模式 |
| UI | React + Tailwind CSS + CSS Variables |
| AI 流 | AI SDK UI 或兼容的结构化 HTTP 流 |
| 数据校验 | Zod 或等价 Schema 库 |
| 前端 UI 状态 | `useReducer` + Context |
| 部署 | 腾讯云 CloudBase Run |
| 初始模型评估 | 火山方舟，通过服务端模型适配层接入 |
| E2E | Playwright |
| 单元测试 | Vitest 或项目模板默认工具 |

浏览器不得直接调用模型，游戏权威状态必须由服务端读取和修改。

## 4. 页面与路由

### 4.1 页面总览

| 路由 | 页面 | 主要用途 |
|---|---|---|
| `/` | 游戏首页 | 介绍游戏、开始或继续 |
| `/intro` | 开场介绍 | 建立角色关系与穿越背景 |
| `/game/[saveId]` | 第一章游戏页 | 完成对话、调查和行动 |
| `/game/[saveId]/result` | 章节结算页 | 展示结局和关键行为 |
| `/feedback` | 试玩反馈页 | 提交结构化反馈 |

不存在、无权限或已删除的 `saveId` 进入存档错误状态，不展示任何其他玩家数据。

### 4.2 首页 `/`

展示：

- 游戏名称、简介和版本号；
- 预计时长“约 15～20 分钟”；
- 内容提示和测试说明；
- 固定背景图；
- “开始游戏”；
- 本机存在有效存档时显示“继续游戏”；
- 环境音开关，默认关闭；
- 页脚隐私和反馈入口。

交互与验收：

- 首次玩家无需注册；
- 点击开始进入开场介绍；
- 有存档时可以继续；
- 已有存档再开始新游戏时必须确认；
- 环境音不得自动播放；
- 手机首屏可见主要行动。

### 4.3 开场介绍 `/intro`

使用 4 个短场景卡：

1. 男主与未婚妻；
2. 洞穴探索任务；
3. 空间异常与机甲坍塌；
4. 归航即将重新上线。

交互与验收：

- 支持上一步、下一步和跳过；
- 可选填写试玩代号；
- 点击“开始穿越”创建匿名会话和第一份存档；
- 重复点击只创建一份存档；
- 不填写代号也能进入；
- 创建失败时保留输入并允许重试；
- 成功后跳转 `/game/[saveId]`。

### 4.4 游戏页 `/game/[saveId]`

页面区域：

1. `GameHeader`：章节、生命、能源和归乡线索；
2. `MessageTimeline`：环境、归航、NPC、玩家和系统消息；
3. `SuggestedActions`：3～4 个参考行动；
4. `InputModeTabs`：询问、执行、NPC 交流；
5. `GameComposer`：自由输入、发送、停止和重试；
6. `GameStateDrawer`：状态、背包、人物、档案、归乡计划；
7. `ActionConfirmation`：不可逆或高成本行动确认；
8. `StreamRecoveryBanner`：断线和错误恢复。

首次进入：

- 校验匿名会话对存档的访问权；
- 加载最近一个已完成回合的快照；
- 存在未完成流时尝试续流；
- 第一回合尚未创建时生成固定开场；
- 滚动至最后一条完整消息。

关键交互：

- 询问不骰点、不擅自推进；
- 执行由服务端判断确认或结算；
- 只有当前存在 NPC 时开放 NPC 交流；
- 快捷行动只填入输入框，玩家可编辑，不立即执行；
- 停止生成不回滚已完成结算；
- 重试不得重复扣资源；
- 查看状态不调用模型、不推进游戏。

异常状态必须分别处理：存档不存在、无权限、已结束、流中断、模型不可用、状态冲突、内容拒绝和网络离线。

### 4.5 结算页 `/game/[saveId]/result`

展示：

- 结局名称和叙述；
- 生命、能源、线索；
- 关键物品得失；
- 石黎关系事实；
- 3～5 条关键行为；
- 是否愿意继续第二章；
- 填写反馈和重开入口。

约束：

- 只有结束存档可以访问；
- 未结束存档返回游戏页；
- 重开需要二次确认；
- P1 不真正进入第二章，只记录继续意愿。

### 4.6 反馈页 `/feedback`

字段：

- 是否理解归乡目标；
- 是否信任归航及原因；
- 最纠结的选择；
- 是否发现状态或剧情矛盾；
- 是否有合理输入被错误理解；
- 是否愿意继续第二章；
- 不愿继续的主要原因；
- 推荐给哪类玩家；
- 可选联系方式。

验收：必填校验、防重复提交、联系方式明确可选、失败保留内容、成功显示感谢。

## 5. 组件清单

### 5.1 布局组件

| 组件 | 职责 |
|---|---|
| `AppShell` | 全局布局、主题和安全区域 |
| `GameShell` | 游戏页头部、消息区和输入区 |
| `MobileViewportGuard` | 处理虚拟键盘和安全区 |
| `PageErrorState` | 页面级错误和恢复入口 |

### 5.2 消息组件

| 组件 | 用途 |
|---|---|
| `MessageTimeline` | 按回合展示并分段加载消息 |
| `NarrationCard` | 环境和叙事 |
| `GuihangMessage` | 归航消息和固定角色标识 |
| `NpcMessage` | NPC 姓名、头像和对话 |
| `PlayerMessage` | 玩家输入及其模式 |
| `SystemResolutionCard` | 骰点、成本和状态变化 |
| `ConfirmationCard` | 高风险行动确认 |
| `ChapterEndCard` | 章末摘要入口 |
| `StreamingCursor` | 流式生成状态 |

### 5.3 输入组件

| 组件 | 职责 |
|---|---|
| `InputModeTabs` | 切换询问、执行、NPC 交流 |
| `GameComposer` | 输入、字数、发送、停止、草稿 |
| `SuggestedActions` | 展示并填入建议行动 |
| `TargetNpcSelector` | 选择交流对象 |
| `SubmitButton` | 发送、等待或重试 |

默认输入上限 500 个中文字符，超限时不提交并说明原因。

### 5.4 状态组件

- `GameHeader`；
- `HealthIndicator`；
- `EnergyIndicator`，显示到 0.1%；
- `ClueIndicator`，显示 `0/5`；
- `GameStateDrawer`；
- `InventoryPanel`；
- `RelationshipPanel`；
- `ArchivePanel`；
- `HomecomingPanel`。

### 5.5 通用组件

- 按钮、模态框、抽屉；
- Toast 与行内提示；
- Loading Skeleton；
- 重试按钮；
- 声音开关；
- 版本标记。

AI 普通文本不能创建可信系统按钮或结算卡片。

## 6. 前端数据结构

### 6.1 公开游戏状态

```ts
type PublicGameState = {
  saveId: string;
  version: number;
  chapter: 1;
  eventId: string;
  phase: GamePhase;
  health: number;
  stamina: number;
  energy: number;
  exposure: number;
  clues: number;
  memoryDamage: number;
  conditions: ConditionView[];
  inventory: InventoryItemView[];
  relationships: RelationshipView[];
  archives: ArchiveEntryView[];
  homecoming: HomecomingView;
  availableNpcIds: string[];
  suggestedActions: SuggestedAction[];
  isChapterComplete: boolean;
  ending?: EndingSummary;
};
```

隐藏危险、NPC 秘密、真实意图、未发现事件和随机种子不得返回前端。

### 6.2 输入与 UI 状态

```ts
type InputMode = 'ask_guihang' | 'execute_action' | 'talk_to_npc';

type GameUiPhase =
  | 'loading'
  | 'ready'
  | 'submitting'
  | 'awaiting_confirmation'
  | 'executing'
  | 'streaming'
  | 'settled'
  | 'retryable_error'
  | 'game_over';
```

### 6.3 消息

```ts
type GameMessage = {
  id: string;
  turnId: string;
  kind: 'narration' | 'guihang' | 'npc' | 'player' | 'system';
  speakerId?: string;
  inputMode?: InputMode;
  parts: GameMessagePart[];
  createdAt: string;
  status: 'complete' | 'streaming' | 'interrupted' | 'error';
};
```

### 6.4 本地保存

浏览器只保存最近 `saveId`、输入草稿、声音/动画偏好和可安全缓存的完整消息。匿名身份使用安全 HttpOnly Cookie 或等价机制；模型密钥、隐藏状态和规则配置不得进入 Local Storage。

## 7. 接口清单

统一错误结构：

```ts
type ApiError = {
  error: {
    code: string;
    message: string;
    retryable: boolean;
    requestId: string;
  };
};
```

### 7.1 创建匿名会话

```http
POST /api/session/anonymous
```

返回 `playerId` 和 `expiresAt`，并写入安全会话 Cookie。有效会话存在时必须幂等返回原会话。

### 7.2 创建存档

```http
POST /api/saves
Idempotency-Key: <uuid>
```

```ts
type CreateSaveRequest = { playerAlias?: string; chapter: 1 };

type CreateSaveResponse = {
  saveId: string;
  state: PublicGameState;
  messages: GameMessage[];
};
```

### 7.3 读取存档

```http
GET /api/saves/:saveId
```

返回公开状态、消息和可选的 `resumableTurn`。只允许当前匿名会话访问自己的存档。

### 7.4 提交玩家输入

```http
POST /api/saves/:saveId/turns
Idempotency-Key: <uuid>
```

```ts
type SubmitTurnRequest = {
  expectedStateVersion: number;
  mode: InputMode;
  text: string;
  targetNpcId?: string;
};
```

响应为结构化消息流：

```ts
type GameStreamPart =
  | { type: 'turn_started'; turnId: string }
  | { type: 'player_message'; message: GameMessage }
  | { type: 'confirmation_required'; confirmation: ConfirmationView }
  | { type: 'narration_delta'; messageId: string; text: string }
  | { type: 'guihang_delta'; messageId: string; text: string }
  | { type: 'npc_delta'; messageId: string; npcId: string; text: string }
  | { type: 'check'; check: ActionCheckView }
  | { type: 'state_patch'; version: number; patch: PublicGameStatePatch }
  | { type: 'choices'; items: SuggestedAction[] }
  | { type: 'chapter_end'; ending: EndingSummary }
  | { type: 'turn_completed'; turnId: string; version: number }
  | { type: 'error'; error: ApiError['error'] };
```

### 7.5 确认高风险行动

```http
POST /api/saves/:saveId/turns/:turnId/confirm
Idempotency-Key: <uuid>
```

请求包含 `confirmationId`、`confirm/cancel` 和预期状态版本。取消不能产生资源结算。

### 7.6 恢复中断流

```http
GET /api/saves/:saveId/turns/:turnId/stream
```

若回合已经完成，返回最终状态；否则续流。不得重新执行规则结算。

### 7.7 重开第一章

```http
POST /api/saves/:saveId/restart
Idempotency-Key: <uuid>
```

保留原存档为结束状态并创建新存档，不直接覆盖。

### 7.8 提交反馈

```http
POST /api/feedback
Idempotency-Key: <uuid>
```

包含 `saveId`、问卷答案、可选联系方式和客户端环境摘要。

## 8. 错误码与前端行为

| 错误码 | 场景 | 前端行为 |
|---|---|---|
| `INVALID_INPUT` | 输入为空、过长或字段错误 | 保留文本并提示修改 |
| `SESSION_EXPIRED` | 匿名会话失效 | 尝试恢复，失败时说明限制 |
| `SAVE_FORBIDDEN` | 不属于当前玩家 | 不展示数据并返回首页 |
| `SAVE_NOT_FOUND` | 存档不存在 | 显示重新开始 |
| `STATE_VERSION_CONFLICT` | 页面状态落后 | 重读状态，不自动重放 |
| `TURN_IN_PROGRESS` | 重复提交 | 连接现有回合流 |
| `ACTION_NOT_FEASIBLE` | 行动不可能 | 显示原因且不扣资源 |
| `RATE_LIMITED` | 请求过快 | 显示等待时间 |
| `MODEL_UNAVAILABLE` | 模型不可用 | 保留结算并允许重试叙事 |
| `GAME_SERVICE_UNAVAILABLE` | 规则/存档不可用 | 不结算，允许稍后重试 |

## 9. 前端状态管理

```ts
type GameClientState = {
  uiPhase: GameUiPhase;
  game: PublicGameState | null;
  messages: GameMessage[];
  inputMode: InputMode;
  inputDraft: string;
  selectedNpcId?: string;
  pendingTurnId?: string;
  pendingConfirmation?: ConfirmationView;
  lastError?: ApiError['error'];
  soundEnabled: boolean;
  stateDrawerOpen: boolean;
};
```

前端禁止自行修改生命、能源、物品、线索、关系和结局。只有校验通过的 `state_patch` 和 `chapter_end` 可以修改显示。

允许乐观显示玩家消息、处理中状态、输入框清空和纯 UI 偏好；不允许乐观显示成功和资源变化。

## 10. 核心交互流程

### 10.1 首次开始

```text
首页 → 开场介绍 → 创建匿名会话 → 创建存档
→ 固定开场消息 → 游戏页 → 等待输入
```

### 10.2 询问归航

```text
选择询问 → 提交问题 → 读取已知资料
→ 归航流式回答 → 权威状态不变 → ready
```

### 10.3 执行普通行动

```text
选择执行 → 提交行动 → 可行性判断 → 服务端骰点
→ 状态结算 → 流式剧情 → 更新建议 → ready
```

### 10.4 执行高风险行动

```text
提交 → 服务端要求确认 → 显示确认卡
→ 玩家确认 → 骰点与结算 → 流式剧情 → ready
```

### 10.5 中断与重试

```text
流中断 → 保留已收片段 → 查询回合
→ 已完成则拉取结果；未完成则续流
→ 不重复结算
```

### 10.6 游戏结束

```text
chapter_end → 禁用普通输入 → 章末卡
→ 结算页 → 记录继续意愿 → 反馈
```

## 11. 响应式、视觉与可访问性

### 11.1 手机端

- 以约 390px 宽度为主要基准；
- 顶部状态栏不超过两行；
- 输入区位于虚拟键盘和安全区上方；
- 状态面板使用底部抽屉；
- 不使用依赖悬停的交互；
- 覆盖微信内置浏览器。

### 11.2 桌面端

- 对话主栏设置最大宽度；
- 状态抽屉可变为右侧栏；
- 支持 Enter 发送、Shift+Enter 换行。

### 11.3 素材

- 首页主视觉 1 张；
- 洞穴/驾驶舱背景 2～3 张；
- 洞口与山寨远景 1 张；
- 归航头像 1 个；
- 石黎头像或剪影 1 个；
- 环境音循环 1～2 条。

素材未完成时使用占位资源，不阻塞页面开发。

### 11.4 可访问性

- 所有交互支持键盘；
- 模式切换有文本标签；
- 危险状态不只依赖颜色；
- 模态框正确管理焦点；
- 流式输出避免逐字符重复朗读；
- 支持 `prefers-reduced-motion`；
- 音效默认关闭；
- 错误、检定和状态变化使用语义化提示。

## 12. 埋点与日志

产品事件：

- `landing_viewed`、`intro_started`、`save_created`；
- `game_started`、`input_submitted`；
- `suggested_action_used`、`free_action_used`；
- `confirmation_shown`、`confirmation_cancelled`；
- `turn_completed`、`stream_recovered`；
- `chapter_completed`、`continue_interest`；
- `feedback_submitted`。

技术日志：请求 ID、匿名玩家 ID、存档 ID、回合 ID、响应时间、流中断、服务错误、版本冲突、幂等请求、Schema 失败和客户端环境摘要。

分析系统不默认记录完整玩家输入；完整对话日志的保存用途和期限必须在试玩说明中告知。

## 13. 测试范围

### 13.1 单元与组件测试

- reducer 阶段转换；
- `state_patch` 合并与版本检查；
- 流式片段解析；
- 消息类型到组件映射；
- 输入校验；
- 确认卡和焦点；
- 状态抽屉；
- 长文本、空文本和错误组件。

### 13.2 E2E 主路径

1. 匿名开始；
2. 询问且状态不变；
3. 执行并收到公开检定；
4. 高风险确认和取消；
5. 与石黎交流；
6. 刷新后继续；
7. 流中断后恢复；
8. 重复点击不重复扣能源；
9. 死亡后结算；
10. 成功逃生并反馈；
11. 两个匿名浏览器上下文存档隔离。

### 13.3 设备

- 微信内置浏览器 iOS/Android；
- iOS Safari；
- Android Chrome/主流 Chromium；
- 桌面 Chrome、Edge、Safari 当前稳定版本。

## 14. 验收标准

### 14.1 页面

| ID | 验收项 | 通过标准 |
|---|---|---|
| FE-PAGE-001 | 首页 | 无登录开始，有存档可继续 |
| FE-PAGE-002 | 介绍 | 可完成或跳过，不重复建档 |
| FE-PAGE-003 | 游戏页 | 五类消息、三种模式、状态抽屉可用 |
| FE-PAGE-004 | 结算页 | 只展示服务端权威结局 |
| FE-PAGE-005 | 反馈页 | 校验、提交、失败保留正常 |

### 14.2 对话与状态

| ID | 验收项 | 通过标准 |
|---|---|---|
| FE-GAME-001 | 询问不执行 | 不触发骰点或资源消耗 |
| FE-GAME-002 | 高风险确认 | 确认后才结算 |
| FE-GAME-003 | 服务端权威 | AI 普通文本不能改状态 |
| FE-GAME-004 | 状态版本 | 旧页面不能覆盖新状态 |
| FE-GAME-005 | 幂等 | 重试不重复扣资源 |
| FE-GAME-006 | 流恢复 | 不重复骰点或结算 |
| FE-GAME-007 | 自由输入 | 合理行动获得明确结果 |
| FE-GAME-008 | 不可行行动 | 不扣资源并说明原因 |
| FE-GAME-009 | 游戏结束 | 禁止继续普通行动 |
| FE-GAME-010 | 存档隔离 | 不能读取他人存档 |

### 14.3 体验与业务

| ID | 验收项 | 通过标准 |
|---|---|---|
| FE-UX-001 | 手机输入 | 键盘不遮挡输入和发送 |
| FE-UX-002 | 响应反馈 | 提交后立即显示处理状态 |
| FE-UX-003 | 动效声音 | 可减少动画，声音默认关闭 |
| FE-UX-004 | 错误恢复 | 常见错误均有操作入口 |
| FE-UX-005 | 长对话 | 完章后仍可平滑滚动输入 |
| FE-BIZ-001 | 漏斗统计 | 可统计开始、完成和退出 |
| FE-BIZ-002 | 归航互动 | 可统计主动询问归航 |
| FE-BIZ-003 | 自由输入 | 可统计自由输入使用率 |
| FE-BIZ-004 | 继续意愿 | 记录第二章意愿 |
| FE-BIZ-005 | 反馈闭环 | 反馈可匿名关联存档 |

## 15. 联调契约

前端可先使用符合本文 Schema 的 Mock Server。切换真实服务前必须完成：

- OpenAPI 或等价接口定义；
- 请求和响应 Schema 共用或自动生成；
- 错误码对齐；
- 流式协议样例；
- 幂等键处理；
- 状态版本冲突演练；
- 模型与规则服务故障降级演练。

接口字段变化必须同步更新类型、Mock、测试和本文档。

## 16. 开发交付物

- Next.js 项目；
- 页面和组件；
- 响应式样式；
- 类型、Schema、Mock 数据和 Mock 接口；
- 真实接口适配；
- 单元、组件和 E2E 测试；
- 不含真实密钥的环境变量示例；
- 部署配置；
- 本地启动、测试、构建和部署 README；
- 已知问题、版本号和变更记录。

## 17. Definition of Done

全部满足才算完成：

- 5 个页面可用；
- 第一章可走到至少四类结局；
- 三种输入模式和高风险确认正常；
- 状态、物品、关系和结局由服务端驱动；
- 刷新、断网和重复提交不破坏存档；
- 中国大陆目标设备验证完成；
- 无高优先级可访问性阻塞；
- Must 功能和关键 E2E 测试通过；
- 腾讯云测试环境生成分享链接；
- 5 名玩家独立完成并提交反馈；
- 关键问题进入下一版本待办。

