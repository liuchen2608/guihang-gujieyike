# 《归航：蛊界异客》前端技术适配声明

> 文档版本：v0.1  
> 文档状态：产品方案已确认，待开发负责人技术评审  
> 适用阶段：P1 网页单章试玩及后续三章 MVP  
> 更新日期：2026-08-25

关联文档：

- [`PRD.md`](./PRD.md)
- [`本阶段前端开发文档.md`](./本阶段前端开发文档.md)

## 1. 声明目的

本文档说明《归航：蛊界异客》网页端采用的前端技术方案、选择原因，以及相对于默认快速原型方案的偏离项。

本文档用于：

- 让产品、设计和开发对实现边界形成共同理解；
- 避免把普通聊天机器人界面直接当作游戏正式前端；
- 约束 AI、游戏规则和浏览器界面之间的职责；
- 为技术评审、开发排期和后续替换模型供应商提供依据。

## 2. 默认方案基线

在本项目中，“默认方案”指：

> 使用扣子、Dify 或同类无代码 Agent 平台提供的托管聊天页面，通过单条主提示词、少量会话变量和平台默认聊天组件生成试玩链接。

默认方案适合 P0 纯对话验证，优点是上线快、无需编写前端代码、便于少量用户测试提示词。

默认方案不作为 P1 以后正式网页版本的唯一技术基础，原因是本项目需要：

- 明确区分“询问”“执行”“与 NPC 交流”；
- 同时展示环境、归航、NPC、玩家和系统结算等多种消息；
- 对生命、能源、物品、关系和线索进行权威结算；
- 支持不可逆行动的确认与防误触；
- 对每位玩家的世界状态进行严格隔离；
- 形成具有游戏感而非客服感的网页体验。

## 3. 正式开发采用方案

### 3.1 技术栈

| 层级 | 采用方案 | 用途 |
|---|---|---|
| 语言 | TypeScript 严格模式 | 降低消息、状态和接口字段不一致风险 |
| Web 框架 | Next.js App Router | 页面路由、服务端能力、接口和部署入口 |
| UI 框架 | React | 构建高交互的对话与状态界面 |
| 样式 | Tailwind CSS + CSS Variables | 手机优先布局、主题和状态色管理 |
| 基础组件 | 可访问性优先的无样式/轻样式组件 | 对话框、抽屉、提示和确认操作 |
| AI 对话传输 | AI SDK UI 的流式消息协议，或与其兼容的自定义传输层 | 流式输出文本、状态片段、工具结算和错误信息 |
| 运行时校验 | Zod 或等价 Schema 校验库 | 校验服务端返回的结构化消息和状态补丁 |
| 前端局部状态 | React `useReducer` + Context | 管理输入模式、抽屉、确认状态和流式 UI |
| 端到端测试 | Playwright | 验证手机/桌面流程、断线、继续游戏和结局 |
| 单元测试 | Vitest 或项目模板默认测试工具 | 验证 reducer、消息解析和显示逻辑 |

依赖版本不在本声明中写死。项目初始化时采用稳定版本，并通过 lockfile 固定；升级必须经过回归测试。

### 3.2 采用 Next.js App Router 的原因

- 首页、产品说明等内容适合服务端渲染；
- 游戏页可以设置清晰的 Client Component 边界；
- Route Handler 可以承接会话请求和流式响应；
- 模型密钥、数据库访问和规则结算可以留在服务端；
- 同一个项目可同时维护落地页、游戏页、结算页和服务端接口。

Next.js App Router 默认使用 Server Components，并支持服务端渲染、加载状态和流式页面传输。本项目仅在需要浏览器交互的游戏区域使用 Client Components。

### 3.3 采用结构化流式消息的原因

本项目返回的不只是 AI 文本，还包括：

- 环境叙述；
- 归航对话；
- NPC 对话；
- 骰点和行动结算；
- 状态变化；
- 快捷行动；
- 错误与重试信息。

因此前端不采用“把所有内容拼成一段 Markdown”的方式，而接收带类型的数据片段，例如：

```ts
type GameStreamPart =
  | { type: 'narration'; text: string }
  | { type: 'guihang'; text: string }
  | { type: 'npc'; npcId: string; text: string }
  | { type: 'check'; result: ActionCheckView }
  | { type: 'state_patch'; patch: PublicGameStatePatch }
  | { type: 'choices'; items: SuggestedAction[] }
  | { type: 'chapter_end'; summary: ChapterSummary }
  | { type: 'error'; code: string; retryable: boolean };
```

AI SDK UI 支持聊天消息流、数据片段、传输层和断线续流，可用于实现上述协议。若后端以后迁移到其他语言或云服务，前端仍通过稳定的 HTTP/流式协议通信。

## 4. 前端架构原则

### 4.1 服务端状态为唯一事实来源

以下状态只能由服务端规则系统确认后更新：

- 生命、体力、能源和身份暴露；
- 物品获得、消耗和损坏；
- NPC 关系事实；
- 归乡线索；
- 当前章节、事件和结局；
- 永久记忆损坏。

浏览器可以显示临时状态，但不能自行决定结算结果。

### 4.2 前端不直接调用模型

浏览器只调用本项目服务端接口。模型供应商密钥、系统提示词、规则工具和隐藏状态不得进入客户端包，也不得保存在 Local Storage。

### 4.3 询问与执行严格分离

游戏输入包含三种明确模式：

```ts
type InputMode = 'ask_guihang' | 'execute_action' | 'talk_to_npc';
```

- `ask_guihang` 默认不推进游戏时间；
- `execute_action` 触发可行性检查、必要确认和规则结算；
- `talk_to_npc` 进入人物交流流程，并根据情景决定是否推进时间。

不可逆或高成本行动必须出现确认卡片，玩家确认后才能提交结算。

### 4.4 游戏页采用有限客户端边界

服务端组件负责：

- 首页和章节入口；
- 初始存档读取；
- 用户身份与会话校验；
- 静态说明和元数据。

客户端组件负责：

- 消息流；
- 输入框和模式切换；
- 流式内容显示；
- 快捷选项；
- 状态抽屉；
- 确认、重试和错误提示。

整个站点不改造成纯客户端单页应用。

### 4.5 前端只保存可恢复标识

浏览器本地最多保存：

- 匿名玩家 ID；
- 当前存档 ID；
- UI 偏好；
- 未发送的输入草稿；
- 已确认可公开的最近消息缓存。

权威存档、隐藏关系、骰点种子、系统提示词和模型密钥必须保存在服务端。

## 5. 相对默认方案的偏离

| 默认方案 | 本项目适配 | 偏离原因 |
|---|---|---|
| 使用平台默认托管聊天页 | 自建游戏网页外壳 | 需要独立品牌、状态栏、档案抽屉、章节结算和游戏化布局 |
| 所有输入都是普通聊天 | 设置询问、执行、NPC 交流三种模式 | 防止咨询被误判为行动，保证不可逆操作可确认 |
| AI 直接生成完整回复 | 规则先结算，AI 再根据结果生成表现 | 防止状态漂移、物品凭空出现和无条件成功 |
| AI 文本是唯一响应 | 使用结构化流式消息 | 同时承载对话、结算、状态补丁和快捷行动 |
| 聊天历史就是游戏记忆 | 独立保存结构化游戏状态和事实摘要 | 长对话会遗忘或矛盾，不能作为权威存档 |
| 浏览器乐观更新状态 | 关键状态等待服务端确认 | 避免显示成功后又回滚，保护永久代价的可信度 |
| 通用 Markdown 渲染 | 按消息类型使用受控组件渲染 | 防止 AI 伪造系统卡片、按钮或状态变化 |
| 一个全局系统提示词控制一切 | 叙事、意图识别、规则结算职责分离 | 降低提示词复杂度并便于测试 |
| 默认桌面聊天框 | 手机优先、全屏沉浸式游戏页 | 首批玩家主要通过分享链接在手机浏览器试玩 |
| 平台会话即玩家身份 | 项目生成独立玩家/存档 ID | 保证单人存档隔离，并为继续游戏提供依据 |
| 模型供应商与页面绑定 | 服务端提供模型适配层 | 便于更换模型、控制成本和实施降级方案 |

## 6. 明确不采用的前端方案

P1/P2 阶段暂不采用：

- 原生 iOS 或 Android 客户端；
- Unity、Unreal 等重型游戏引擎；
- WebGL、3D 场景或实时动作战斗；
- 微前端；
- WebSocket 作为默认对话通道；
- Redux 等全局状态库作为首版必需依赖；
- 把模型 API Key 放在浏览器中；
- 由前端随机生成决定生命、物品和剧情结局；
- 将未经校验的 AI HTML 直接插入页面。

说明：默认采用基于 HTTP 的流式响应。只有后续出现双向实时事件、语音或服务器主动推送需求时，再评估 WebSocket。

## 7. 页面与组件边界

### 7.1 路由建议

```text
/                     游戏首页
/intro                开场介绍
/game/[saveId]        对话游戏页
/game/[saveId]/result 章节结算页
/feedback             试玩反馈页
```

### 7.2 核心组件

- `GameHeader`：章节和公开状态；
- `MessageTimeline`：消息时间线；
- `NarrationCard`：环境叙述；
- `GuihangMessage`：归航消息；
- `NpcMessage`：NPC 消息；
- `SystemResolutionCard`：检定和资源变化；
- `SuggestedActions`：快捷行动；
- `InputModeTabs`：输入模式切换；
- `GameComposer`：自由输入和提交；
- `ActionConfirmation`：不可逆行动确认；
- `GameStateDrawer`：背包、关系、档案和归乡计划；
- `StreamRecoveryBanner`：断线续传或重试；
- `ChapterSummary`：章末结算。

### 7.3 前端 UI 状态

```ts
type GameUiPhase =
  | 'ready'
  | 'submitting'
  | 'awaiting_confirmation'
  | 'executing'
  | 'streaming'
  | 'settled'
  | 'retryable_error'
  | 'game_over';
```

使用 reducer 管理上述有限状态，禁止在多个组件中各自维护互相矛盾的提交和流式标志。

## 8. 响应式和可访问性要求

- 以 390px 左右宽度的手机视口作为主要设计基准；
- 首批用户位于中国大陆，P1 必须覆盖微信内置浏览器以及主流移动端 Chromium/WebKit 环境；
- 输入区适配虚拟键盘和安全区域；
- 桌面端对话主栏设置最大宽度，状态抽屉可转为侧栏；
- 所有按钮支持键盘操作和清晰焦点样式；
- 不能只依赖颜色表示生命危险或关系变化；
- 流式文字更新需避免屏幕阅读器逐字符重复朗读；
- 支持减少动态效果偏好；
- 关键检定、资源变化和错误信息使用语义化提示区域。

## 9. 性能与稳定性要求

- 首页尽量保持静态和轻量；
- 游戏首屏先展示已有消息和输入框，再连接流式服务；
- 长对话采用窗口化或分段加载，避免 DOM 无限增长；
- 玩家发送行动后立即显示“已提交”，但不乐观修改权威状态；
- 流式中断时保留已收到内容，并提供继续或重试；
- 重试请求必须带幂等标识，避免同一行动结算两次；
- 页面刷新后从服务端恢复最后一个已完成回合；
- 未完成的流式回合不得成为正式存档状态。

## 10. 安全边界

- 模型密钥和服务端令牌不得出现在浏览器网络响应或构建产物中；
- 所有结构化响应必须通过 Schema 校验后渲染；
- AI 输出默认作为文本渲染，不允许任意 HTML；
- 服务端验证玩家是否拥有对应存档；
- 状态变更接口需要防重放与幂等处理；
- 日志避免保存不必要的个人信息；
- 试玩代号不应被当成真实身份认证。

## 11. 测试要求

至少覆盖：

- 首页进入、开始游戏、继续游戏；
- 三种输入模式不会互相混淆；
- 不可逆行动必须确认；
- AI 流式消息正常结束；
- 中途断网后可以恢复或安全重试；
- 同一行动不会重复扣除能源；
- 刷新页面后状态与服务端一致；
- 不同匿名玩家之间存档隔离；
- 死亡后不能继续提交普通行动；
- 手机虚拟键盘不会遮挡输入框；
- 长对话不会导致页面明显卡顿；
- AI 伪造的“系统消息”不会被当成真实结算卡片。

## 12. 分阶段采用策略

### P0：无代码对话原型

- 可继续使用托管 Agent 页面；
- 目的仅为验证提示词、归航人格和第一章选择；
- 不作为状态可信度和正式 UI 的验证依据。

### P1：自建网页单章试玩

- 采用本文声明的 Next.js + React + TypeScript 方案；
- 建立服务端权威状态和结构化流；
- 完成第一章及 5 人测试。

### P2：三章 MVP

- 扩展存档、档案、归乡计划和记忆损坏；
- 根据数据决定是否增加账号和云端跨设备存档；
- 保持模型供应商可替换。

## 13. 待确认项

以下事项必须在开发排期前由产品发起人确认：

1. 前端视觉主题和品牌素材；
2. 开发人员是否已有必须遵循的公司级前端模板。

已确认：P0 只使用无代码托管页面完成内部提示词与玩法验证；P1 单章试玩直接采用自建 Next.js 网页。

已确认：P1 允许匿名试玩，自动生成玩家 ID 和存档 ID；同一浏览器可以继续游戏，暂不支持账号登录和跨设备存档。

已确认：P1 使用少量固定背景图、归航头像、轻量打字动画和默认关闭的环境音；不加入实时 AI 生图、角色配音和复杂动画。

已确认：首批试玩用户位于中国大陆，部署和模型方案以大陆网络可用性为优先条件。

### 13.1 已确认部署方案

- P1 全栈 Next.js 应用部署到腾讯云 CloudBase Run；其支持容器化 Node.js/任意框架应用、自动伸缩和系统分配域名，适合包含服务端接口与流式响应的试玩应用。
- 初始模型接入优先评估火山方舟；其提供国内模型服务、对话/Responses API 和 Function Calling，可用于让模型提出结构化工具调用，而游戏规则仍由项目服务端执行。
- 保留模型适配层，不把前端、存档格式或规则系统绑定到某个具体模型。
- P1 封闭测试可先使用云平台提供的临时访问地址；自有域名与中国大陆正式公开服务应预留 ICP 备案时间，并由接入服务商确认实际流程。
- 对外发布生成式 AI 内容前，需与模型服务商和专业合规人员确认内容治理、用户协议、隐私、算法备案/资质材料等具体要求。

产品已确认按上述方案推进。开发负责人可以基于实际账户、报价和技术验证提出等价替代，但不得降低中国大陆访问稳定性、密钥安全和模型可替换性。

## 14. 参考依据

- [Next.js App Router：服务端渲染、导航与 Streaming](https://nextjs.org/docs/app/getting-started/linking-and-navigating)
- [Next.js：服务端数据访问与 Streaming](https://nextjs.org/docs/app/getting-started/fetching-data)
- [AI SDK UI：useChat 与流式聊天状态](https://ai-sdk.dev/docs/reference/ai-sdk-ui/use-chat)
- [AI SDK UI：Stream Protocols](https://ai-sdk.dev/docs/ai-sdk-ui/stream-protocol)
- [腾讯云 CloudBase Run：容器化应用托管](https://cloud.tencent.com/document/product/876/121989/)
- [火山方舟：模型服务与 API 能力](https://www.volcengine.com/docs/82379/?lang=zh)
- [火山方舟 Responses API：Function Calling](https://www.volcengine.com/docs/82379/1958524?lang=zh)
- [工业和信息化部：非经营性互联网信息服务备案管理办法](https://www.miit.gov.cn/gyhxxhb/jgsj/cyzcyfgs/bmgz/xxtxl/art/2024/art_84a0cfa0ebd049bbbe751dca9a008e56.html)

---

## 15. 声明结论

本项目正式网页版本采用“服务端权威游戏状态 + React 对话界面 + 结构化 AI 流式消息”的方案。

相对于默认无代码聊天页，本项目增加了自建游戏 UI、输入模式隔离、规则结算、独立存档和严格消息类型校验。这些偏离会提高初期开发成本，但属于保证单人 AI 对话 RPG 可控、可信和可持续扩展所必需的适配。
